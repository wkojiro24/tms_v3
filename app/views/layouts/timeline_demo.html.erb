<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Timeline Demo</title>
    <style>
      body {
        margin: 0;
        font-family: "Noto Sans JP", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        background: #f5f7fb;
        color: #1f2937;
      }
      header {
        padding: 1rem 2rem;
        background: white;
        border-bottom: 1px solid #e5e7eb;
      }
      main {
        padding: 2rem;
      }
      .card {
        background: white;
        border: 1px solid #e5e7eb;
        border-radius: 12px;
        box-shadow: 0 5px 20px rgba(15, 23, 42, 0.05);
        margin-bottom: 1.5rem;
      }
      .card-body {
        padding: 1.25rem;
      }
    </style>
    <%= yield :head %>
  </head>
  <body>
    <header>
      <h1 style="margin:0;font-size:1.25rem;">Timeline Demo</h1>
    </header>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
