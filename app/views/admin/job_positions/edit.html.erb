<% content_for :title, "役職編集" %>
<% content_for :section_label, "マスタ管理" %>

<div class="card card-lift">
  <div class="card-body">
    <h2 class="h5 mb-3">役職を編集</h2>
    <%= render "form", job_position: @job_position %>
    <%= link_to "一覧へ戻る", admin_job_positions_path, class: "btn btn-link mt-3" %>
  </div>
</div>
