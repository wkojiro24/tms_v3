<% content_for :title, "ユーザー編集" %>
<% content_for :section_label, "承認管理" %>

<div class="card card-lift">
  <div class="card-header bg-white fw-semibold">ユーザー情報の編集</div>
  <div class="card-body">
    <%= render "form" %>
  </div>
</div>
