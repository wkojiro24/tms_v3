<% content_for :title, "項目編集" %>
<% content_for :section_label, "マスタ管理" %>

<div class="card card-lift">
  <div class="card-body">
    <h2 class="h5 mb-3"><%= @category.display_label %> の項目を編集</h2>
    <%= render "form", category: @category, item: @item %>
  </div>
</div>
