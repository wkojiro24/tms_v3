<% content_for :title, "職種編集" %>
<% content_for :section_label, "マスタ管理" %>

<div class="card card-lift">
  <div class="card-body">
    <h2 class="h5 mb-3">職種を編集</h2>
    <%= render "form", job_category: @job_category %>
    <%= link_to "一覧へ戻る", admin_job_categories_path, class: "btn btn-link mt-3" %>
  </div>
</div>
