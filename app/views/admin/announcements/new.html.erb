<% content_for :title, "お知らせ作成" %>
<% content_for :section_label, "管理者" %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h4 class="mb-0">お知らせ作成</h4>
</div>

<%= render "form" %>
