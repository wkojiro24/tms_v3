<% content_for :title, "従業員編集" %>
<% content_for :section_label, "従業員管理" %>

<div class="card card-lift">
  <div class="card-body">
    <h2 class="h5 mb-3">従業員情報を編集</h2>
    <%= render "form" %>
  </div>
</div>
